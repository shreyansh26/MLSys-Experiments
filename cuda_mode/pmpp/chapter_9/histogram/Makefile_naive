NVCC        = /usr/bin/nvcc
NVCC_FLAGS  = -O3 -I/usr/include
LD_FLAGS    = -lcudart -L/usr/lib64
EXE	        = naive
OBJ	        = naive.o

default: $(EXE)

naive.o: naive.cu
	$(NVCC) -c -o $@ naive.cu $(NVCC_FLAGS)

$(EXE): $(OBJ)
	$(NVCC) $(OBJ) -o $(EXE) $(LD_FLAGS)

clean:
	rm -rf *.o $(EXE)